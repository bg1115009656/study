spring:
  application:
    name: spring-boot-redis
  jpa:
      database: mysql
      show-sql: true
  datasource:
    druid:
      username: root
      password: root
      url: jdbc:mysql://localhost:3306/test
  redis:
    password: